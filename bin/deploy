#!/usr/bin/env bash
set -e

yarn run build

cd build


git init
git checkout -b gh-pages
git add --all
git commit -m "deploy"
git push --force --quiet "git@github.com:realsee-developer/realsee-developer.github.com.git" gh-pages:gh-pages